# discollection-web

Next.js web viewer for JSON output generated by the `discollection` CLI `organize` command.

## Features

- Drag and drop or upload an organized JSON export from the CLI.
- Browse the collection by format and section in the side panel.
- Search releases by title, artist, labels, genres, and styles.
- Explore the collection graph on the infinite canvas.
- Copy `release-override` commands from release cards when you want to adjust metadata in the CLI.
- Switch light/dark theme.

## Development

From monorepo root:

```bash
pnpm install
pnpm --filter discollection-web dev
```

Open http://localhost:3000.

## Build And Lint

From monorepo root:

```bash
pnpm --filter discollection-web build
pnpm --filter discollection-web lint
pnpm --filter discollection-web start
```

## Input Data

Generate input JSON with CLI:

```bash
pnpm --filter discollection dev organize ./tmp/organized.json
```

Then upload `./tmp/organized.json` in the web UI.

The app does not read the SQLite database directly; it expects the organized JSON export generated by the CLI.

## Deployment Notes

- Framework: Next.js
- Root Directory (Vercel): `discollection-web`
- Install command: `pnpm install --frozen-lockfile`
- Build command: `pnpm --filter discollection-web build`
